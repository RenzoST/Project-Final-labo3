import { defineConfig } from 'vite'

export default defineConfig({
  base: 'https://RenzoST.github.io/Project-Final-labo3', // Reemplaza <USERNAME> con tu nombre de usuario de GitHub
  build: {
    outDir: 'dist', // Esto es donde Vite guardará los archivos de construcción
  }
})
